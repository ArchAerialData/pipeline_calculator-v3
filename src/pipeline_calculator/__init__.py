"""Pipeline Calculator package (refactor-in-progress).

For now, the canonical implementation lives in `src/pipeline_calculator_v3.py`.
This package provides a stable namespace to migrate modules into incrementally.
"""

__version__ = "0.0.0"

