[pytest]
testpaths = tests
addopts = -ra
markers =
    native_gui(timeout=45, traceback_timeout=20): native Tk scenario isolated in a subprocess with a hard deadline and earlier diagnostic traceback

