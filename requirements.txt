pyproj>=3.4.0
pandas>=1.5.0
# Verified together on Windows and Apple Silicon macOS 15/26/27. Older SciPy
# PROPACK wheels fail to load on macOS 27; keep the numerical stack reproducible.
numpy==1.26.4
scipy==1.17.1
# GUI adapters are verified against this version's scrolling/binding internals.
customtkinter==5.2.2
pyinstaller>=5.0.0
tkinterdnd2
Pillow>=9.0.0
openpyxl>=3.1.0
shapely>=2.0,<3
